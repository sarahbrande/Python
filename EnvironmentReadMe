environment:

select anaconda (from start menu), which then launches Jupyter that can interpret each line.
Shift enter runs line of code.

+ adds another cell, used arrow to move it.  b adds cell below a adds cell aboveescape gets you out of edit mode.

control enter to interpret but not advance ot a new line.

github good place to find packages (how do you know which ones work/tested? download

to install package @ anaconda command prompt (lesson 19)
conda install packagename or
 pip install package name.

import package or function (from package import function)
 -- it seemed to work so it must have known to get it from github

on a import of a package, if you type the first part then . then tab, it gives a list of possibilities.
